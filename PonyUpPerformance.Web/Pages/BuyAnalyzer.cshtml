@page
@model PonyUpPerformance.Web.Pages.BuyAnalyzerModel

@{
    ViewData["Title"] = "Buy Analyzer";

    var stoplight = new PonyUpPerformance.Web.Models.PonyUpStoplightResultViewModel
    {
        HasResult = Model.Result != null,
        AnalysisType = "ANALYSIS STATUS",
        Recommendation = Model.Result?.Recommendation ?? "READY",
        ConfidenceScore = Model.Result?.ConfidenceScore ?? 0,
        Reasoning = Model.Result?.Reasoning ?? "Enter the vehicle information and run the buy analysis."
    };
}

<section class="page-section analyzer-page buy-bg">
    <h1 class="section-title">BUY ANALYZER</h1>
    <p class="section-subtitle">Should you buy this vehicle, negotiate harder, or walk away?</p>

    <div class="price-card analyzer-card"
     style="max-width:900px;margin:0 auto;">
        <form method="post">
            <label>Year</label>
            <input asp-for="Input.Year" />

            <label>Make</label>
            <input asp-for="Input.Make" />

            <label>Model</label>
            <input asp-for="Input.Model" />

            <label>Mileage</label>
            <input asp-for="Input.Mileage" />

            <label>Asking Price</label>
            <input asp-for="Input.AskingPrice" />

            <label>Estimated Repair Cost</label>
            <input asp-for="Input.EstimatedRepairCost" />

            <label>Market Value</label>
            <input asp-for="Input.MarketValue" />

            <button class="btn-red" type="submit" style="margin-top:20px;">
                ANALYZE BUY
            </button>
        </form>
    </div>

    <partial name="_PonyUpStoplightResult" model="stoplight" />
</section>
